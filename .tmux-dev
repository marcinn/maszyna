#!/bin/sh


tmux new-session -d
tmux send-keys "vim" C-m
tmux rename-window 'code'

tmux new-window
tmux rename-window 'testenv'
tmux send-keys "cd /opt/Maszyna2203/" C-m

tmux new-window
tmux rename-window 'other'

tmux select-window -t :=0
tmux select-pane -D

tmux -2 attach-session -d
